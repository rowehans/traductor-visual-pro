# Traductor Visual Pro - Python Dependencies
# Core
flask==3.1.3
opencv-python-headless==5.0.0.93
numpy==2.5.1
pillow==12.3.0

# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1

# OCR
easyocr==1.7.2
torch==2.6.0+cu124

# RapidOCR (ONNX) — pipeline híbrido EasyOCR+RapidOCR
# Usa los mismos modelos PP-OCRv4 que PaddleOCR pero via ONNX Runtime,
# sin conflictos de PaddlePaddle con PyTorch.
#
# IMPORTANTE: onnxruntime (CPU) es SUFICIENTE para RapidOCR.
# El benchmark en GTX 1050 Ti demostró que GPU no acelera porque los
# modelos son pequeños (~1.5MB detector + ~5MB recognizer) y el overhead
# de transferencia PCIe anula cualquier ganancia.
#
# Para instalar versión GPU (opcional, SIN mejora real):
#   pip install onnxruntime-gpu
#
# pip resuelve onnxruntime (CPU) automaticamente como dependencia de rapidocr-onnxruntime
# CPU es suficiente — ver nota IMPORTANTE arriba
rapidocr-onnxruntime==1.3.8

# Translation
argostranslate==1.11.0
deep-translator==1.11.4
langdetect==1.0.9
langcodes==3.3.0
ctranslate2==4.8.1
transformers==5.14.0
huggingface_hub==1.23.0
sentencepiece==0.2.1

# Database
flask-sqlalchemy==3.1.1
flask-limiter==4.1.1

# OCR spellchecking (86K palabras, cero mantenimiento)
pyspellchecker==0.9.0

# Utilities
requests==2.34.2
